version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: hydrafly-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  postgres:
    image: postgres:16-alpine
    container_name: hydrafly-postgres
    environment:
      POSTGRES_USER: hydrafly
      POSTGRES_PASSWORD: hydrafly
      POSTGRES_DB: hydrafly
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/sql:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hydrafly"]
      interval: 5s
      timeout: 3s
      retries: 5

  mongo:
    image: mongo:7
    container_name: hydrafly-mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: hydrafly
      MONGO_INITDB_ROOT_PASSWORD: hydrafly
      MONGO_INITDB_DATABASE: hydrafly_raw
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  redis_data:
  postgres_data:
  mongo_data:
